# abort script on any command that exits with a non zero value
set -e

cd ${BUILD_DIR}

curl -L -o windows_buildpack_app_lifecycle.tgz https://ci.appveyor.com/api/buildjobs/5ppowt3e22nrp8sf/artifacts/output/windows_app_lifecycle-03360d1.tgz

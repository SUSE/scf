#!/bin/sh

set -o errexit
set -o nounset
set -x

PATH=$PATH:/var/vcap/packages/cli/bin

cf api --skip-ssl-validation "https://api.$DOMAIN"
cf auth admin "${CLUSTER_ADMIN_PASSWORD}"
cf enble-feature-flag diego_docker

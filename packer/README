# packer boxes for vagrant

## Requirements

packer: https://www.packer.io/
txtplate: https://github.com/aarondl/txtplate/releases

## Creation

To create one of the boxes do this

```bash
packer build -only=<ONE OF: vagrant-virtualbox, vagrant-libvirt, vagrant-vmware, jenkins-slave, scf-cached> vagrant-box.json
```
